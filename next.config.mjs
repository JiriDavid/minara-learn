/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "placehold.co",
      "images.unsplash.com",
      "plus.unsplash.com",
      "example.com",
    ],
  },
};

export default nextConfig;
